apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: mediconnect
spec:
  selector:
    app: api-gateway
  ports:
    - port: 4000
      targetPort: 4000
      nodePort: 30400      # access via: minikube ip + :30400
  type: NodePort
