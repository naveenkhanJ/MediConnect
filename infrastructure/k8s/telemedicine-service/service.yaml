apiVersion: v1
kind: Service
metadata:
  name: telemedicine-service
  namespace: mediconnect
spec:
  selector:
    app: telemedicine-service
  ports:
    - protocol: TCP
      port: 5005
      targetPort: 5005
