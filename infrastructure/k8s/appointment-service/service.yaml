apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: mediconnect
spec:
  selector:
    app: appointment-service
  ports:
    - port: 5003
      targetPort: 5003
  type: ClusterIP
